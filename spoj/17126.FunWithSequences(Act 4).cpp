#include <iostream>
#include <cstdio>
#include <altgorithm>

using namespace std;

int main(int argc, char const *argv[])
{
	int n, x, y;

	scanf("%d %d", &n, &x);
	int s[n]
	for (int i = 0; i < n; ++i)
	{
		scanf("%d", &s[i]);
	}
	for (int i = 0; i < m; ++i)
	{
		scanf("%d", &q[i]);
	}

	sort(s, s+n);
	sort(q, q+n);


	return 0;
}


5 2
-1 -1 -1 1 2 
-2 -1  1 2 3
1 3 4 5 

6 4
-1  2 2 2 2 -2 
 3 -2 3 3 3 -1